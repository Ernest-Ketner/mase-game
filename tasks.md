# Ощущение забега и развитие листа

## A — P0
- [x] `js/audio.js` — Web Audio, без файлов
- [x] HUD на холсте (жизни, оружие, метки, банк, охота, тег)
- [x] Лист 1: только grunt, без стража и чемпиона
- [x] Выстрел по метке не берёт её, клякса + звук

## B — P1
- [x] Волны scout / pressure / hunt в `js/spawn.js`
- [x] Теги листа с 4-го + сид `?seed=`
- [x] Магазин 2/3, редкость, оффер под оружие
- [x] Стеки апгрейдов + 6 ключевых
- [x] Цель 20 листов, штамп, забег продолжается

## C — P2
- [x] Знаки видов врагов + первая подсказка
- [x] `js/fx.js`: брызги, пластина, урон, тряска
- [x] Пустой автомат/дробь → гарантированный дроп; «Карандаш»

## D — P3
- [x] Рукописный шрифт в меню/HUD
- [x] Демо-меню: метка + grunt, Играть = новый лист 1

## E — техника
- [x] Спавн вынесен из `main.js`
- [x] Апгрейды данными
- [x] `test/invariants.js` + html
- [x] Правила `.cursor/rules/web-game.mdc`
- [x] Коммит только файлов игры

## F — золото, лимит врагов, таймер по центру

### F1 — золото (`js/run.js`, `js/main.js`, `js/fx.js`, `js/render.js`)
- [x] `run.gold = 0` в `createRunState`; новый забег и «Ещё раз в этом сиде» обнуляют
- [x] `noteKill` → `run.gold += GOLD_PER_KILL` (10), подбирать не нужно
- [x] FX: «+10» чернилами всплывает над врагом и уходит к счётчику; строка «золото N» в правом столбце HUD моргает (`hudPulse.gold`)
- [x] Константы `GOLD_PER_KILL = 10`, `LIFE_PRICE = 900`, `REROLL_PRICE = 100` в одном месте

### F2 — покупка жизни на окне поражения (`index.html`, `js/main.js`)
- [x] Кнопка «Купить жизнь — 900 (есть N)» в `#death-panel` над «Ещё раз»
- [x] Неактивна, если золота < 900; скрыта, если причина — «Вышел с листа»
- [x] Покупка: `gold -= 900`, `lost = false`, HP = максимум, 2 с неуязвимости (`grantInvuln`), закрыть окно, игра дальше с того же места
- [x] Если умер от срока: +20 с к таймеру, снять `overtime` и урон по сроку
- [x] Рекорд уже записан в `showDefeat` — повторно не портить

### F3 — обновить карточки за 100 (`js/upgrades.js`, `js/main.js`, `index.html`)
- [x] Кнопка «Обновить — 100» в `#upgrade-panel` (и в прокачке после листа, и в «Добыче»)
- [x] `pickOffers(..., { exclude })` — новые карточки не повторяют текущие, если в каталоге есть замена
- [x] Столько же карточек, сколько было; метки из банка не тратятся
- [x] Неактивна при золоте < 100; клавиша `R` в окне прокачки
- [x] После обновления строка золота в оверлее обновляется

### F4 — лимит врагов на лист (`js/spawn.js`)
- [x] `sheetEnemyCap(levelNum) = 10 + 5 * (levelNum - 1)`; счётчик `spawner.total` сбрасывается в `resetSpawner`
- [x] Считать каждого поставленного в очередь в `queueSpawn`: свита, страж, чемпион, засады `a1`, стартовые паки
- [x] Исключение: чемпион для цели «босс» выходит даже при исчерпанном лимите
- [x] `queuePack`: сильный враг только если влезает вместе со свитой, иначе grunt или ничего
- [x] Проверка: `killQuota` (≤14) всегда ≤ лимита листа (с листа 2 лимит 15) — цель «зачистка» выполнима
- [x] Когда лимит исчерпан, таймер спавна просто молчит; «охота» остаётся режимом поведения живых

### F5 — таймер по центру сверху (`js/render.js`, `js/main.js`)
- [x] `hud.clock` (срок) и отсчёт «выжить» → по центру сверху, шрифт ~24px, красный при `overtime`
- [x] Оба сразу: срок крупно, под ним «выжить 0:24» чуть мельче
- [x] В правом столбце у цели «выжить» остаётся только название
- [x] Штампы «охота» / «время» опускаются под таймер, чтобы не наезжать

### F6 — завершение
- [x] Обновить `.cursor/rules/web-game.mdc` (золото, цены, лимит врагов, таймер)
- [x] `test/invariants.js`: лимит на лист, чемпион-босс сверх лимита, `killQuota` ≤ лимита
- [x] Прогон в браузере: убийство → +10, обновление карточек, покупка жизни после смерти и после срока
